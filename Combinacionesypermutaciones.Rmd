---
title: "Combinaciones y permutaciones"
author: "Leslie Martínez Montero"
date: "2023-12-05"
output: pdf_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## Combinaciones y permutaciones 

# Instalación de paquetería 

1. Instalar paquetería **gtools**
  
```{r}
install.packages("gtools")

```
 2. Abrir librería 

```{r}
library(gtools)
```

# COMBINACIONES

 1. Ejemplo: Tenemos 100 estudiantes y se quieren agrupar 
 en equipos de 2 integrantes cada uno. 

 N = 100 - Número de elementos 

 n = 2 - Grupos de 2 en 2 

```{r}
N <- 100 
n <- 2 
```


 2. Determinar que vayan los números con ID número 
 consecutivo. En esta ocasión, el objeto se llamará "alumnos". 

```{r}
alumnos <- c(1:N)

```

## Cómo sacar las combinciones 

 1. Nombrar un objeto llamado "combinaciones", utilizando 
 **N**, **n** y **alumnos** y la función **combinations**
  
```{r}
combinaciones <- combinations(N, n, alumnos)

```

 2. Las primeras combinaciones

```{r}
head(combinaciones)

```

 3. Las últimas combinaciones

```{r}
tail(combinaciones)

```

 4. ¿Cuántas combinaciones tenemos en total?
  
```{r}
nrow(combinaciones)

```

 5. Utilizando la fórmula 
 **factorial(N) / (factorial(n) * (factorial(N-n)))**
 debe salir el mismo número que en el código anterior

```{r}
factorial(N) / (factorial(n) * (factorial(N-n)))

```
# Mi ejercicio

 1. Se quieren realizar equipos de 3 integrantes con 
 los alumnos de primer y segundo grado de la escuela 
 primaria "Emiliano Zapata" de Xalapa. En total se 
 tienen 120 alumnos. 

 N = 120 - Número de elementos 

 n = 3 - Grupos de 3 en 3

```{r}
N <- 120
n <- 3
```

 2. Son los alumnos con id un número consecutivo

```{r}
alumnos <- c(1:N) 

```

## Sacar las combinaciones 

 1. Nombrar un objeto llamado "combinaciones2", 
 utilizando **N**, **n** y **alumnos** y la función 
 **combinations**

```{r}
combinaciones2 <- combinations(N, n, alumnos)

```

2. Las primeras combinaciones

```{r}
head(combinaciones2)

```

 3. Las últimas combinaciones

```{r}
tail(combinaciones2)

```

 4. ¿Cuántas combinaciones tenemos en total?

```{r}
nrow(combinaciones2) 

```

 5. Utilizando la fórmula 
 **factorial(N) / (factorial(n) * (factorial(N-n)))**
 debe salir el mismo número que en el código anterior

```{r}
factorial(N) / (factorial(n) * (factorial(N-n)))

```

# PERMUTACIONES 

 1. Ejemplo: Tenemos 100 estudiantes y se quieren agrupar
 en equipos de 2 integrantes cada uno. 

 N = 100 - Número de elementos 

 n = 2 - Grupos de 2 en 2

```{r}
N <- 100 
n <- 2 
```

 2. Determinar que vayan los números con ID número 
 consecutivo. En esta ocasión, el objeto se llamará "alumnos". 

```{r}
alumnos <- c(1:N) 

```

#Sacar las permutaciones 

 1. Nombrar un objeto llamado "permutaciones", utilizando 
 **N**, **n** y **alumnos** y la función **permutations**

```{r}
permutaciones <- permutations(N, n, alumnos)

```

 2. Las primeras permutaciones

```{r}
head(permutaciones) 

```

 3. Las últimas permutaciones

```{r}
tail(permutaciones)
```

4. ¿Cuántas permutaciones se tienen en total?

```{r}
nrow(permutaciones)

```

5. Utilizando la fórmula 
 **factorial(N) / factorial(N-n)**,debe salir el 
 mismo número que en el código anterior

```{r}
factorial(N) / factorial(N-n) 

```

# Mi ejercicio

 1. Se quieren realizar equipos de 3 integrantes con los
 alumnos de primer y segundo grado de la escuela primaria 
 "Lázaro Cárdenas de Banderilla. En total se tiene 150 alumnos. 

 N = 150 - Número de elementos 

 n = 3 - Grupos de 3 en 3

```{r}
N <- 150
n <- 3 
```

2. Son los alumnos con id un número consecutivo

```{r}
alumnos <- c(1:N)

```

## Sacar las permutaciones

 1. Nombrar un objeto llamado "permutaciones2", utilizando
 **N**, **n** y **alumnos** y la función **permutations**
  
```{r}
permutaciones2 <- permutations(N, n, alumnos)

```

2. Las primeras permutaciones

```{r}
head(permutaciones2)
```

3. Las últimas permutaciones

```{r}
tail(permutaciones2)

```

4. ¿Cuántas permutaciones se tienen en total?
  
```{r}
nrow(permutaciones2)

```

 5. Utilizando la fórmula
 **factorial(N) / factorial(N-n)**,debe salir el mismo
 número que en el código anterior

```{r}
factorial(N) / factorial(N-n)

```


